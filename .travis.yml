language: ruby
rvm:
- 2.4.5

before_install:
   - nvm install 8.12.0
   - npm install -g yarn
   - yarn
   - gem install bundler

cache:
  directories:
  - $HOME/.yarn-cache

script: ./script/cibuild

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

sudo: false
