language: ruby
rvm:
- 2.4.1

before_install:
   - nvm install 6.14.3
   - npm install -g yarn
   - yarn

cache:
  directories:
  - $HOME/.yarn-cache

script: ./script/cibuild

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

sudo: false
