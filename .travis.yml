language: ruby
rvm:
- 2.4.5

before_install:
   - nvm install 10.15.0
   - npm install -g yarn
   - yarn

cache:
  directories:
  - $HOME/.yarn-cache

script: ./script/cibuild

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

sudo: false
